# vim: ft=conf

# status line
set-option -g status-left-length 32
set-option -g status-left "[#S]"
set-option -g status-right-length 64
set-option -g status-right "%A, %B %d | %I:%M %p | #(hostname -s)"
